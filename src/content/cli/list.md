# List

The `fenn list` command shows all available template directories in the templates repository.

## Usage

```bash
fenn list
```

## Examples

### Basic Usage

List all available template directories in the templates repository:

```bash
fenn list
```
